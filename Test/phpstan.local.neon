includes:
    - phpstan.neon
    - phar:///var/www/html/vendor/phpstan/phpstan/phpstan.phar/conf/bleedingEdge.neon

parameters:
    treatPhpDocTypesAsCertain: false
    magento:
        magentoRoot: ../..  # Points to /var/www/html; resolved relative to CWD at analysis time (module root), not this file's location
    excludePaths:
        analyseAndScan:
            - ../.github
            # Stub file defines minimal Magento class skeletons for CI-only use;
            # scanning it would override PHPStan's real Magento type knowledge.
            - Stubs
        analyse:
            - psalm-stubs.stub
            - phpstan-stubs.stub
